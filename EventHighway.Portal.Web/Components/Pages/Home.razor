@page "/"

<PageTitle>Health Dashboard</PageTitle>

<div class="d-flex justify-content-between align-items-center py-3">
    <h2 class="mb-0">Health Dashboard</h2>
    <span class="badge text-bg-secondary">EventHighway</span>
</div>

<RagTileRow />

<div class="card mb-4">
    <div class="card-body">
        <p class="mb-0">
            Welcome to the EventHighway operations portal. The RAG tiles above are live from the
            health clients. The remaining dashboard panels (traffic chart, usage tables, loop /
            duplicate / retry panels) are being built next.
        </p>
    </div>
</div>
